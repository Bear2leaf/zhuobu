{
  "name": "openai-quickstart-node",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "type": "module",
  "scripts": {
    "compile": "npx tsc -w & npx tsc -w -p ..",
    "reload": "browser-sync start --proxy localhost:3100 --files '../dist'",
    "nodemon": "nodemon --watch dist/**/*.js --exec node dist/app.js",
    "start": "npm run nodemon & npm run reload & npm run compile"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.0",
    "browser-sync": "^2.29.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "openai": "^3.2.1",
    "typescript": "^5.0.4"
  }
}
