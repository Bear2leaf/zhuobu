import AnimationController from "../controller/AnimationController.js";

export default class Animator  {
    animate(controller: AnimationController): void {
        throw new Error("Method not implemented.");
    }
}